import type { IGeneral } from '@/interfaces/IGeneral';
import BR from './BR';
import EN from './EN';

const homeTexts: { [key: string]: IGeneral } = {
  "BR": BR,
  "EN": EN,
}

export default homeTexts;