import type { ITextQuiz } from '@/interfaces/IQuizes';
import BR from './BR';
import EN from './EN';

const footbalClub2018CupTexts: { [key: string]: ITextQuiz } = {
  "BR": BR,
  "EN": EN,
}

export default footbalClub2018CupTexts;